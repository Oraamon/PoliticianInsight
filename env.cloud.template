# Template de .env para Google Cloud
# Este arquivo mostra como as variáveis devem ser configuradas no Google Cloud
# NÃO use este arquivo diretamente - configure as variáveis no console/CLI

# =============================================================================
# CLOUD RUN - Configuração via gcloud CLI:
# =============================================================================
# gcloud run services update seu-servico \
#   --set-env-vars GEMINI_API_KEY=sua_chave_aqui,PORT=8080 \
#   --region=us-central1

# Ou usando Secret Manager (recomendado):
# gcloud run services update seu-servico \
#   --update-secrets GEMINI_API_KEY=gemini-api-key:latest \
#   --region=us-central1

# =============================================================================
# APP ENGINE - Configuração via app.yaml:
# =============================================================================
# Edite app.yaml e configure:
# env_variables:
#   GEMINI_API_KEY: "sua_chave_aqui"
#   PORT: "8080"

# =============================================================================
# DESENVOLVIMENTO LOCAL:
# =============================================================================
# Copie env.example para .env e preencha:
# cp env.example .env
# 
# GEMINI_API_KEY=sua_chave_aqui
# PORT=3000

